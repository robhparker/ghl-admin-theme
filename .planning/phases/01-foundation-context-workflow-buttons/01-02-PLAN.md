---
phase: 01-foundation-context-workflow-buttons
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/ghl-customizer.js
  - test/run.mjs
  - test/dom-shim.mjs
autonomous: true
requirements: [LOC-01, LOC-02, LOC-03, LOC-04, LOC-05, BTN-04, BTN-05, BTN-06, BTN-07, BTN-10, BTN-11, BTN-12, DLV-04]
tags: [vanilla-js, spa-navigation, history-api, generation-token, webhook, cooldown]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
assumption_delta:
  decision: no-change
  rationale: "Same finding as Plan 01: the scan's 'additional' hit in ROADMAP success criterion 2 is a count constraint (one webhook event per click, none while submitting or cooling down). No second variant, optional anchor, or chosen parameter is introduced; the (locationId, contactId, requestId) identity model is unchanged."
must_haves:
  truths:
    - "After navigating between contacts or locations by in-app pushState/replaceState, browser back/forward (popstate), or HighLevel's routeChangeEvent, the contact button on screen is bound to the contact currently in the URL and the previous element is gone (LOC-02, BTN-06)"
    - "Every context change increments the generation token; a webhook result that resolves after the context changed is discarded and never touches the DOM (D-10, LOC-05)"
    - "Clicking Send Invite after the URL changed without any event refuses to fire, shows an unavailable message, and sends nothing (BTN-05)"
    - "Agency-level routes resolve to a null location and remove every customizer button; a location dashboard without a contact shows no contact button (LOC-03, BTN-04)"
    - "Repeated clicks while submitting, and clicks during the post-queued cooldown (default 10 s, configurable per button), produce no additional webhook requests (BTN-12)"
    - "A CORS/network TypeError on the first POST triggers exactly one no-cors retry and reports queued with the label 'Sent (unconfirmed)'; a non-2xx response reports failed with an actionable message that excludes the URL and payload (D-11, BTN-11)"
    - "A contact record with neither email nor phone readable renders the webhook button unavailable with 'contact email/phone not found' and sends nothing on click (D-02, BTN-10)"
    - "With ?ghlc-debug=1 active, console output across the ok/500/cors/offline flows contains no webhook URL, payload, email, or phone (DLV-04)"
  artifacts:
    - path: "src/ghl-customizer.js"
      provides: "installNavigationHooks, scheduleContextCheck, stale-click revalidation, sendWebhook with no-cors fallback, cooldown timers, data-ghlc-reason"
      contains: "installNavigationHooks"
    - path: "test/run.mjs"
      provides: "nav:*, stale:*, ctx:*, webhook:*, logs:* scenarios"
      contains: "webhook-discarded"
    - path: "test/dom-shim.mjs"
      provides: "setPath, setContact, setSidebarMode helpers"
      contains: "setContact"
  key_links:
    - from: "history.pushState / history.replaceState (patched)"
      to: "window event ghlc:navigate"
      via: "wrapper calls the original then dispatches CustomEvent(adapter.events.navigate)"
      pattern: "adapter\\.events\\.navigate"
    - from: "popstate / routeChangeEvent / ghlc:navigate listeners"
      to: "applyContext"
      via: "scheduleContextCheck coalesces into one zero-delay timer"
      pattern: "scheduleContextCheck"
    - from: "runWebhook click handler"
      to: "adapter.parseRoute(location.pathname)"
      via: "click-time revalidation against data-ghlc-ctx and data-ghlc-generation"
      pattern: "stale-click"
    - from: "sendWebhook"
      to: "fetch mode no-cors"
      via: "TypeError on the cors attempt triggers exactly one no-cors retry"
      pattern: "no-cors"
  prohibitions:
    - requirement_id: LOC-04
      category: safety
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT persist location or contact context in Web Storage or cookies; each tab keeps in-memory state only"
    - requirement_id: BTN-06
      category: safety
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT reuse a button element or a pending timer across a context change; the previous contact ID is never carried into the next render"
    - requirement_id: DLV-04
      category: privacy
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT log webhook URLs, request payloads, contact email, or contact phone in any state transition or failure path"
    - requirement_id: BTN-11
      category: privacy
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT include the webhook URL or payload in any user-visible failure message"
---

## Phase Goal

**As a** staff member with a contact record open, **I want to** press one button that triggers the right HighLevel workflow for exactly that contact and location, **so that** the invite goes out once, to the right person, with no stale context and no duplicate sends.

<objective>
Expand the Plan 01 tracer into the full context engine and the hardened webhook path. Task 1 makes the script follow every navigation path HighLevel exposes (pushState/replaceState hooks, popstate, `routeChangeEvent`), stamps every context change with a generation token, and makes the contact button refuse to fire when the contact or location on screen is not the one it was rendered for. Task 2 adds the D-11 no-cors fallback, the BTN-12 cooldown and re-fire guards, D-02's unavailable state for records without email/phone, and proves DLV-04 log hygiene across every outcome.

Purpose: ROADMAP success criteria 2 and 3 become true and automated.
Output: updated `src/ghl-customizer.js` (context + buttons sections), new scenarios in `test/run.mjs`, small shim helpers in `test/dom-shim.mjs`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-context-workflow-buttons/01-CONTEXT.md
@.planning/phases/01-foundation-context-workflow-buttons/01-SKELETON.md
@.planning/phases/01-foundation-context-workflow-buttons/01-01-SUMMARY.md
@src/ghl-customizer.js
@test/dom-shim.mjs
@test/run.mjs
@test/fixtures/config.json

<interfaces>
<!-- From Plan 01 (use as-is): -->
state = { config, generation, ctx: { locationId, contactId, isAgency }, timers: Map<Element, timeoutId>, ready }
applyContext(reason)            bumps state.generation only when locationId/contactId actually changed (or reason === 'boot'), clears state.timers, calls renderAll()
renderPlacement(placement)      reconciles the single .ghlc-group inside the mount by data-ghlc-button-id and data-ghlc-ctx
createButtonEl(button)          builds <button data-ghlc-button-id data-ghlc-placement data-ghlc-ctx="loc|contact" data-ghlc-generation data-state>
setState(el, state, { label, message })
onButtonClick(button, el)       returns early unless data-state is ready or failed
runWebhook(button, action, el)  builds payload, awaits sendWebhook, compares generation before touching the DOM
sendWebhook(url, payload)       -> { outcome: 'ok' | 'failed', message? }
log(event, fields) / warn(event, fields)   fields pass through safe(): booleans, numbers, ID-shaped strings only
adapter.events.routeChange = 'routeChangeEvent'; adapter.events.navigate = 'ghlc:navigate'
GHLC.__test = { parseRoute, resolveButtons, isSafeHttpsUrl, validateConfig, boot, getState, applyContext, adapter }

<!-- Shim (Plan 01): -->
createShim({ pathname, search, origin, configUrl, fixture }); shim.buildShell({ sidebarMode, contact }); shim.run(src)
shim.navigate(path, { via }); shim.back(); shim.forward(); shim.flush(); shim.advanceTimers(ms)
shim.setFetchMode('ok'|'error'|'cors'|'offline'|'hold'); shim.releaseFetch(); shim.fetchLog; shim.setConfigResponse({ status, body })
shim.console.lines; shim.listenerCount(target, type); shim.observers()

<!-- Added by THIS plan (Task 1) to test/dom-shim.mjs: -->
shim.setPath(path)              updates location.pathname/href silently — no history entry, no event (simulates a URL change the script did not observe)
shim.setContact(contactOrNull)  replaces the contents of main: contact -> a fresh .hl_contact-details-header with mailto/tel anchors built from { email?, phone? }; null -> a dashboard placeholder. Returns the new region or null
shim.setSidebarMode('location'|'agency')

<!-- Added by THIS plan to src/ghl-customizer.js: -->
state.hooksInstalled: boolean; state.navTimer: timeoutId|null
installNavigationHooks()        once; patches history.pushState/replaceState; adds popstate, routeChangeEvent, ghlc:navigate listeners
scheduleContextCheck(reason)    clears + sets state.navTimer = setTimeout(() => applyContext(reason), 0)
sendWebhook(url, payload)       -> { outcome: 'ok' | 'unconfirmed' | 'failed' | 'network', message? }
data-ghlc-reason="no-contact-fields"   set on a webhook button rendered unavailable because neither email nor phone was readable
log events added: nav, stale-click, webhook-queued, webhook-unconfirmed, webhook-failed, webhook-network
GHLC.__test.renderAll()         test-mode hook (Task 2): calls renderAll() directly with NO generation bump — the only trigger tests use to force a re-render without a context change
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Follow every navigation path and rebind the contact button to the contact on screen</name>
  <reversibility rating="reversible">History patching and listener wiring are contained in installNavigationHooks; swapping the signal set later is a local edit with no consumer contract.</reversibility>
  <files>src/ghl-customizer.js, test/run.mjs, test/dom-shim.mjs</files>
  <read_first>
    - src/ghl-customizer.js (context and buttons sections as shipped by Plan 01 — extend, do not restructure)
    - test/dom-shim.mjs (history/location/event implementation you are adding helpers to)
    - test/run.mjs (scenario() helper and the tracer scenarios to mirror)
    - .planning/phases/01-foundation-context-workflow-buttons/01-CONTEXT.md (D-09, D-10, route-change signal row of the selector table)
  </read_first>
  <action>
    **src/ghl-customizer.js — context section.** Add `state.hooksInstalled = false` and `state.navTimer = null`. Implement `installNavigationHooks()`: return if already installed; capture `const origPush = history.pushState; const origReplace = history.replaceState;` and replace each with a plain function that calls the original with the same `this` and arguments, then dispatches `new CustomEvent(adapter.events.navigate, { detail: { method: 'pushState' | 'replaceState' } })` on `window` inside a try/catch (a dispatch failure must never break HighLevel's router), and returns the original's return value. Add `window.addEventListener` for `'popstate'`, `adapter.events.routeChange`, and `adapter.events.navigate`, all routed to `onNavigationSignal(evt)` which calls `scheduleContextCheck(evt.type)`. `scheduleContextCheck(reason)`: `clearTimeout(state.navTimer)`; `state.navTimer = setTimeout(() => { state.navTimer = null; applyContext(reason); }, 0)`. The zero-delay coalescing is deliberate (LOC-02): HighLevel's router updates the URL before it renders the new view, and several signals fire for one navigation; one check after the current task queue drains is enough and avoids double renders. `state.navTimer` is NOT stored in `state.timers` (which `applyContext` clears). Call `installNavigationHooks()` from `boot()` right after `state.config` is set, before `applyContext('boot')`. Add `hooksInstalled` to `getState()`.

    `applyContext(reason)` keeps its Plan 01 contract (D-09: in-memory only; D-10: generation bump per change) and additionally logs `nav` with `{ reason, generation }` when a change is applied. LOC-03: when `parseRoute` reports an agency route, `ctx.locationId` and `ctx.contactId` are null, so `resolveButtons` yields `[]` and every group is emptied — no branding work here (Phase 2 owns logo fallback).

    **Buttons section — click-time revalidation (BTN-05).** At the top of `runWebhook`, before reading contact fields: `const live = adapter.parseRoute(location.pathname)`; split `el.dataset.ghlcCtx` on `|` into `boundLocation` and `boundContact` (empty string means null); if `live.locationId !== boundLocation || (live.contactId || '') !== boundContact || el.dataset.ghlcGeneration !== String(state.generation)`: `setState(el, 'unavailable', { message: 'Context changed — reopen the contact and try again' })`, `log('stale-click', { buttonId: button.id, generation: state.generation })`, then `scheduleContextCheck('stale-click')` so the DOM catches up with the URL, and return without fetching. BTN-06: `renderPlacement`'s reconcile already removes elements whose `data-ghlc-ctx` differs; confirm it also removes elements when `desired` is empty and that `applyContext` clears `state.timers` before `renderAll()` so no cooldown timer from a previous contact can later flip a new element.

    **test/dom-shim.mjs.** Add `shim.setPath(path)`, `shim.setContact(contactOrNull)`, and `shim.setSidebarMode(mode)` per the interfaces block. `setContact` must build the region exactly like `buildShell` does (same class names and anchor shapes) so adapter selectors keep matching, and must replace the previous region node (never mutate it in place) so `isConnected` on the old node turns false. Make sure `shim.navigate(path, { via: 'pushState' })` calls `window.history.pushState` through the *current* property value (so the customizer's patch is exercised), that `via: 'popstate'` / `back()` / `forward()` update `location` first and then dispatch a `PopStateEvent`-like `Event('popstate')`, and that `via: 'routeChangeEvent'` updates `location` and dispatches `new CustomEvent('routeChangeEvent')` on `window`.

    **test/run.mjs — scenarios (all use the Plan 01 fixture; boot at `/v2/location/locA/contacts/detail/c1` with `contact: { email: 'jane@example.test', phone: '+15555550100' }` unless stated):**
    1. `nav: pushState to another contact rebinds the button and bumps generation` — capture `old = sendInvite element`; `shim.navigate('/v2/location/locA/contacts/detail/c2', { via: 'pushState' })`; `shim.setContact({ email: 'c2@example.test' })`; `await shim.flush()`; assert exactly one `[data-ghlc-button-id="sendInvite"]` in the document, its `data-ghlc-ctx === 'locA|c2'`, `old.isConnected === false`, `old !== new`, `getState().generation === 2`.
    2. `nav: browser back and forward rebind through popstate` — continue from a c1 -> c2 push; `shim.back()`; `shim.setContact({ email: 'jane@example.test' })`; flush; assert ctx `locA|c1` and generation 3; `shim.forward()`; `shim.setContact({ email: 'c2@example.test' })`; flush; assert ctx `locA|c2`, generation 4.
    3. `nav: routeChangeEvent alone is honored` — `shim.navigate('/v2/location/locA/contacts/detail/c3', { via: 'routeChangeEvent' })`; `setContact({ phone: '+15555550103' })`; flush; assert ctx `locA|c3`.
    4. `nav: replaceState is hooked` — `via: 'replaceState'` to c4 with a rebuilt region; assert ctx `locA|c4`.
    5. `nav: patched pushState still updates location and history` — capture `len = window.history.length`; call `window.history.pushState({}, '', '/v2/location/locA/dashboard')` directly; assert `window.location.pathname === '/v2/location/locA/dashboard'` and `window.history.length === len + 1`.
    6. `nav: leaving the contact removes the button and clears pending timers` — click Send Invite (mode ok), flush -> `queued`; `shim.navigate('/v2/location/locA/dashboard', { via: 'pushState' })`; `shim.setContact(null)`; flush; assert zero `[data-ghlc-button-id]` in the document and `getState().buttons.length === 0`; `await shim.advanceTimers(10000)`; assert still zero buttons and `shim.fetchLog.length === 1`.
    7. `nav: agency route yields a null location and zero buttons` — `shim.navigate('/v2/agency/dashboard', { via: 'pushState' })`; `shim.setSidebarMode('agency')`; `shim.setContact(null)`; flush; `s = getState()`; assert `s.locationId === null`, `s.contactId === null`, `s.isAgency === true`, `s.buttons.length === 0`; also assert `parseRoute('/agency_dashboard/overview').isAgency === true` and `parseRoute('/v2/location/locB/settings')` equals `{ locationId: 'locB', contactId: null, isAgency: false }`.
    8. `stale: click after an unobserved URL change refuses to fire` — `shim.setPath('/v2/location/locA/contacts/detail/c9')` (no event, no region change); `button.click()`; assert immediately `data-state === 'unavailable'`, `.ghlc-btn__msg` text contains `Context changed`, `shim.fetchLog.length === 0`; flush; assert the stale element is no longer connected (the scheduled check re-rendered for c9, whose region still shows jane's fields, so a fresh element bound to `locA|c9` exists — assert its `data-ghlc-ctx`).
    9. `stale: in-flight result from an older generation is discarded` — `createShim({ ..., search: '?ghlc-debug=1' })`; `setFetchMode('hold')`; click -> `submitting`; `shim.navigate(c2, { via: 'pushState' })`; `setContact({ email: 'c2@example.test' })`; flush; assert the new c2 button is `ready`; `shim.releaseFetch()`; flush; assert the new button is still `ready` (not `queued`), the old element is disconnected and its `data-state` is still `submitting` (never updated), `shim.fetchLog.length === 1`, and `shim.console.lines.some(l => l.includes('webhook-discarded'))`.
    10. `ctx: unchanged URL does not bump generation` — dispatch `routeChangeEvent` twice with no URL change; flush; assert generation is still 1.
    11. `ctx: hooks are installed exactly once` — call `GHLC.__test.boot()` a second time is not required; instead assert `shim.listenerCount(window, 'popstate') === 1`, `listenerCount(window, 'routeChangeEvent') === 1`, `listenerCount(window, 'ghlc:navigate') === 1`, and `getState().hooksInstalled === true`.
  </action>
  <verify>
    <automated>node test/run.mjs</automated>
    <fails_when>non-zero exit, or the final stdout line does not begin with "PASS", or any line begins with "not ok - nav:" / "not ok - stale:" / "not ok - ctx:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `node test/run.mjs` exits 0; output contains `ok - nav: pushState to another contact`, `ok - nav: browser back and forward`, `ok - nav: routeChangeEvent alone`, `ok - nav: replaceState is hooked`, `ok - nav: agency route`, `ok - stale: click after an unobserved URL change`, `ok - stale: in-flight result`, `ok - ctx: hooks are installed exactly once`.
    - `grep -c 'installNavigationHooks' src/ghl-customizer.js` -ge 2 (defined and called from boot).
    - `grep -c "'popstate'" src/ghl-customizer.js` -ge 1; `grep -c 'adapter.events.routeChange' src/ghl-customizer.js` -ge 1; `grep -c 'adapter.events.navigate' src/ghl-customizer.js` -ge 2 (dispatched by the history patch and listened to).
    - `grep -c 'scheduleContextCheck' src/ghl-customizer.js` -ge 3 (defined, called from the signal handler, called from the stale-click path).
    - `grep -c 'stale-click' src/ghl-customizer.js` -ge 1 and `grep -c 'Context changed' src/ghl-customizer.js` -ge 1.
    - `grep -c 'localStorage' src/ghl-customizer.js` == 0; `grep -c 'sessionStorage' src/ghl-customizer.js` == 0; `grep -c 'document.cookie' src/ghl-customizer.js` == 0 (LOC-04 / D-09).
    - `grep -c 'setContact' test/dom-shim.mjs` -ge 1; `grep -c 'setPath' test/dom-shim.mjs` -ge 1; `grep -c 'setSidebarMode' test/dom-shim.mjs` -ge 1.
    - `node --check src/ghl-customizer.js` exits 0.
  </acceptance_criteria>
  <done>All four navigation signals rebind the contact button to the contact in the URL, generation strictly increases per real change, stale clicks refuse to fire, in-flight results from an older generation never touch the DOM, agency routes clear every button, and `node test/run.mjs` proves each of these. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Webhook hardening — cooldown, no-cors fallback, failed/unavailable messaging, log hygiene</name>
  <reversibility rating="reversible">All behavior is inside sendWebhook/runWebhook/setState; the payload contract from Plan 01 is unchanged.</reversibility>
  <files>src/ghl-customizer.js, test/run.mjs</files>
  <read_first>
    - src/ghl-customizer.js (buttons section: runWebhook, sendWebhook, setState, createButtonEl, state.timers)
    - test/run.mjs (scenario helper; the tracer webhook scenarios to extend)
    - test/dom-shim.mjs (fetch modes ok/error/cors/offline/hold and advanceTimers — no changes expected)
    - .planning/phases/01-foundation-context-workflow-buttons/01-CONTEXT.md (D-02, D-11, constraints on diagnostics)
  </read_first>
  <action>
    **sendWebhook(url, payload) — D-11.** Attempt 1: `fetch(url, { method: 'POST', mode: 'cors', credentials: 'omit', headers: { 'Content-Type': 'application/json' }, body })`. `res.ok` -> `{ outcome: 'ok' }`. Non-ok -> `{ outcome: 'failed', message: 'Workflow did not accept the request (HTTP ' + res.status + '). Try again or contact your admin.' }`. If attempt 1 throws and the error's `name === 'TypeError'` (network/CORS failure): attempt 2 = `fetch(url, { method: 'POST', mode: 'no-cors', credentials: 'omit', body })` with NO Content-Type header (a no-cors request may only carry CORS-safelisted headers; the browser would silently drop a JSON content type, so do not pretend to send one — leave a one-line comment explaining this). Attempt 2 resolving (the response is opaque; `ok` is false and `status` is 0 by design) -> `{ outcome: 'unconfirmed' }`. Attempt 2 throwing -> `{ outcome: 'network', message: 'Could not reach the workflow. Check your connection and try again.' }`. Any non-TypeError throw on attempt 1 -> `{ outcome: 'failed', message: 'Something went wrong sending the request. Try again.' }`. No message ever contains the URL, the payload, or any field of it.

    **runWebhook result mapping + cooldown (BTN-11, BTN-12).** After the generation/isConnected guard: `ok` -> `setState(el, 'queued')` (label "Workflow triggered"), `log('webhook-queued', { buttonId, generation, cooldownMs })`; `unconfirmed` -> `setState(el, 'queued', { label: 'Sent (unconfirmed)', message: 'The workflow endpoint did not confirm receipt. Check the workflow execution log before resending.' })`, `log('webhook-unconfirmed', ...)`; `failed` / `network` -> `setState(el, 'failed', { message })`, `log('webhook-failed' | 'webhook-network', { buttonId, status })` where `status` is the numeric HTTP status or 0. For both queued variants start the cooldown: `cooldownMs = Number.isFinite(action.cooldownMs) && action.cooldownMs >= 0 ? action.cooldownMs : DEFAULT_COOLDOWN_MS`; `id = setTimeout(() => { state.timers.delete(el); if (el.isConnected && el.dataset.ghlcGeneration === String(state.generation)) setState(el, 'ready'); }, cooldownMs)`; `state.timers.set(el, id)`. While `queued` the element is `disabled`, so browsers never deliver clicks; `onButtonClick` additionally returns early for any state other than `ready`/`failed` (BTN-07 repeat-click guard). `failed` leaves the element enabled so the next click starts a fresh attempt with a fresh `requestId`.

    **Unavailable at render for missing contact fields (D-02, BTN-10).** In `createButtonEl`, when the resolved action kind is `webhook` and placement is `contact`: `region = adapter.findContactRegion()`; if `region` is null or both `readContactEmail` and `readContactPhone` return null -> `el.setAttribute('data-ghlc-reason', 'no-contact-fields')` and `setState(el, 'unavailable', { message: 'contact email/phone not found' })`; do not attach the click handler's fetch path (the element is disabled anyway). In `renderPlacement`'s reconcile, when a surviving element has `data-ghlc-reason="no-contact-fields"`, re-run the readability check and, if a field is now readable, remove the attribute, attach the click listener if not yet attached (track with `data-ghlc-bound="1"`), and `setState(el, 'ready')` — HighLevel may render the toolbar before the email field, and Plan 03's observers will call `renderPlacement` again when that happens. `runWebhook` keeps its click-time re-read (fields can also disappear).

    **Re-render test hook + generation consistency (D-10, BTN-05).** Expose `renderAll` on `GHLC.__test` (test mode only, alongside the Plan 01 hooks). Rule, stated here so Task 1's stale-click check and this task's scenarios agree: reconcile never re-stamps `data-ghlc-generation` on a surviving element. Survivors exist only when the context did not change, and `applyContext` bumps the generation only on a real context change (or `reason === 'boot'`, which production runs exactly once against an empty DOM), so a survivor's stamp always equals `state.generation` in production. Tests that need a re-render without a context change MUST call `GHLC.__test.renderAll()` (the same path the Plan 03 observers drive through `renderPlacement`), never `applyContext('boot')` — a forced boot re-apply bumps the generation without changing the context, leaves survivors stamped with the old value, and the stale-click rule then correctly refuses the next click.

    **Payload rules (already in Plan 01; assert them now).** extraFields are copied first (primitives only), core keys assigned last, `email`/`phone` omitted when null, `requestId` fresh per click.

    **DLV-04 static gate.** In `test/run.mjs` add a static check: take the source with the constants section and the verify section removed and assert it contains no `console.` occurrence — every diagnostic goes through `log`/`warn` (whose `safe()` filter is the only path to the console) or through `verify()`.

    **test/run.mjs — scenarios (boot at locA/c1 with jane's email+phone unless stated; fixture `sendInvite.cooldownMs` is 3000):**
    1. `webhook: queued stays disabled for cooldownMs then returns to ready` — click, flush -> `queued` + `disabled`; `advanceTimers(2999)` -> still `queued`, `disabled`; `advanceTimers(1)` -> `ready`, not `disabled`, label `Send Invite`.
    2. `webhook: triple click sends exactly one request` — `click(); click(); click();` synchronously, flush; `fetchLog.length === 1`; state `queued`.
    3. `webhook: click during cooldown sends nothing` — after queued, `click()`, flush, `advanceTimers(100)`; `fetchLog.length === 1`.
    4. `webhook: non-2xx -> failed with an actionable message; retry sends a new requestId` — `setFetchMode('error')`; click; flush; assert `failed`, not `disabled`, label `Failed — retry`, msg contains `HTTP 500` and contains neither `hooks/` nor `TEST-HOOK` nor `c1`; `setFetchMode('ok')`; click; flush; assert `queued`, `fetchLog.length === 2`, `fetchLog[0].bodyJson.requestId !== fetchLog[1].bodyJson.requestId`.
    5. `webhook: CORS TypeError falls back to no-cors and reports Sent (unconfirmed)` — `setFetchMode('cors')`; click; flush; assert `fetchLog.length === 2`, `fetchLog[0].mode === 'cors'`, `fetchLog[1].mode === 'no-cors'`, `fetchLog[1]` has no content-type header, both bodies parse to the same `requestId`; state `queued`, label `Sent (unconfirmed)`, `disabled`; `advanceTimers(3000)` -> `ready`.
    6. `webhook: offline -> failed after both attempts` — `setFetchMode('offline')`; click; flush; `fetchLog.length === 2`; state `failed`; msg contains `connection`; not `disabled`.
    7. `webhook: record without email or phone renders unavailable and sends nothing` — `buildShell({ sidebarMode: 'location', contact: {} })`; boot; assert `data-state === 'unavailable'`, `data-ghlc-reason === 'no-contact-fields'`, msg `contact email/phone not found`, `disabled`; `click()`; flush; `fetchLog.length === 0`.
    8. `webhook: fields appearing later flip unavailable to ready on re-render` — continue from 7: append a `mailto:` anchor into the region via `shim.el`, then call `GHLC.__test.renderAll()` (a re-render with no generation bump — Plan 03's observers drive the same path automatically; the survivor's `data-ghlc-generation` still equals `state.generation`, so the click below passes the stale-click check); flush; assert the same element (`===`) is now `ready`, has no `data-ghlc-reason`, and `getState().generation` is unchanged from before the call; click; flush; `fetchLog.length === 1` and `bodyJson.email` is the appended address.
    9. `webhook: email-only record sends email and omits phone` — contact `{ email: 'only@example.test' }`; click; flush; `bodyJson.email === 'only@example.test'`; `Object.prototype.hasOwnProperty.call(bodyJson, 'phone') === false`.
    10. `webhook: extraFields cannot override identity keys` — `bodyJson.contactId === 'c1'` and `bodyJson.source === 'ghlc-harness'` (the fixture's `contactId: "SHOULD-NOT-WIN"` loses).
    11. `webhook: non-https URL renders unavailable` — `setConfigResponse({ status: 200, body: fixture with sendInvite.action.url = 'http://services.leadconnectorhq.com/hooks/TEST-HOOK' })`; boot; assert `unavailable`, msg contains `HTTPS`; click; `fetchLog.length === 0`.
    12. `logs: debug mode never prints URL, payload, email, or phone` — `search: '?ghlc-debug=1'`; run the ok flow, then `setFetchMode('error')` + click after `advanceTimers(3000)`, then `setFetchMode('cors')` + click after another cooldown; `text = shim.console.lines.join('\n')`; assert `text.includes('[ghlc]')` (logging happened) and that `text` contains none of: `hooks/`, `TEST-HOOK`, `@example`, `5550100`, `"contactId"`, `requestId`, `sentAt`.
  </action>
  <verify>
    <automated>node test/run.mjs</automated>
    <fails_when>non-zero exit, or the final stdout line does not begin with "PASS", or any line begins with "not ok - webhook:" / "not ok - logs:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `node test/run.mjs` exits 0; output contains `ok - webhook: queued stays disabled`, `ok - webhook: triple click`, `ok - webhook: CORS TypeError falls back`, `ok - webhook: offline`, `ok - webhook: record without email or phone`, `ok - webhook: non-https URL`, `ok - logs: debug mode never prints`.
    - `grep -c "mode: 'no-cors'" src/ghl-customizer.js` == 1 (exactly one no-cors retry site) and `grep -c "mode: 'cors'" src/ghl-customizer.js` == 1.
    - `grep -c 'Sent (unconfirmed)' src/ghl-customizer.js` -ge 1 and `grep -c 'Workflow triggered' src/ghl-customizer.js` -ge 1 (BTN-11 wording present).
    - `grep -c 'Email delivered' src/ghl-customizer.js` == 0 (BTN-11 forbids delivery wording).
    - `grep -c 'contact email/phone not found' src/ghl-customizer.js` -ge 1 and `grep -c 'no-contact-fields' src/ghl-customizer.js` -ge 2 (set and re-checked).
    - `grep -c 'DEFAULT_COOLDOWN_MS' src/ghl-customizer.js` -ge 2 (declared and used) and `grep -c 'cooldownMs' src/ghl-customizer.js` -ge 2.
    - `grep -c 'webhook-queued' src/ghl-customizer.js` -ge 1; `grep -c 'webhook-failed' src/ghl-customizer.js` -ge 1.
    - `grep -c 'renderAll' src/ghl-customizer.js` -ge 3 (defined, called from applyContext, exposed on GHLC.__test) and `grep -c 'renderAll()' test/run.mjs` -ge 1 (webhook:8 forces its re-render through the hook, not through a generation bump).
    - `node --check src/ghl-customizer.js` exits 0.
  </acceptance_criteria>
  <done>Repeat clicks and cooldown never double-send; CORS failures degrade to one no-cors retry with honest "Sent (unconfirmed)" feedback; non-2xx and offline show actionable failed messages without the URL or payload; records without email/phone render unavailable and recover when fields appear; debug logging across every outcome leaks nothing. Committed.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 02 share)

| Kind | Name | Where |
|---|---|---|
| Function | `installNavigationHooks()` | context section; patches `history.pushState`/`replaceState`, listens to `popstate`, `routeChangeEvent`, `ghlc:navigate` |
| Function | `scheduleContextCheck(reason)` | context section; zero-delay coalesced `applyContext` |
| Function | `onNavigationSignal(evt)` | context section |
| State | `state.hooksInstalled`, `state.navTimer` | context section |
| Event | `ghlc:navigate` (`CustomEvent`, `detail.method`) | dispatched on `window` after every pushState/replaceState |
| Function | `sendWebhook(url, payload)` -> `{ outcome: ok|unconfirmed|failed|network, message? }` | buttons section |
| Behavior | cooldown timer in `state.timers` keyed by element; cleared on context change | buttons section |
| DOM | `data-ghlc-reason="no-contact-fields"`, `data-ghlc-bound="1"` | contact webhook buttons |
| Messages | `Context changed — reopen the contact and try again`; `contact email/phone not found`; `Workflow did not accept the request (HTTP n). Try again or contact your admin.`; `Could not reach the workflow. Check your connection and try again.`; `The workflow endpoint did not confirm receipt. Check the workflow execution log before resending.` | `.ghlc-btn__msg` / `title` |
| Labels | `Sent (unconfirmed)` (queued via no-cors) | `.ghlc-btn__label` |
| Log events | `nav`, `stale-click`, `webhook-queued`, `webhook-unconfirmed`, `webhook-failed`, `webhook-network` | `[ghlc]` console prefix; IDs/states/numbers only |
| Symbol | `GHLC.__test.renderAll()` | test mode only; re-render with no generation bump (survivors keep a current stamp) |
| Shim | `shim.setPath`, `shim.setContact`, `shim.setSidebarMode` | `test/dom-shim.mjs` |
| Tests | `nav:*`, `stale:*`, `ctx:*`, `webhook:*`, `logs:*` scenarios; console-usage static gate | `test/run.mjs` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HighLevel router -> script | URL and route events are the only source of location/contact identity; they can change at any time, including mid-request |
| script -> Inbound Webhook | Each click becomes one public POST; duplicates and misattributed sends are the primary harm |
| script -> console | Failure paths are where URLs and payloads most often leak into diagnostics |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | stale context -> wrong contact enrolled | high | mitigate | Click-time revalidation of `data-ghlc-ctx` + `data-ghlc-generation` against `adapter.parseRoute(location.pathname)`; generation compare after every await; elements never reused across contexts (scenarios stale:1, stale:2, nav:1) |
| T-01-09 | Denial of Service / Tampering | double-send via rapid clicks or resend during cooldown | medium | mitigate | `disabled` while submitting/queued, `onButtonClick` state guard, per-button cooldown (`cooldownMs`, default 10 s) tracked in `state.timers` (scenarios webhook:1-3) |
| T-01-03 | Information Disclosure | failure messages and debug logs | medium | mitigate | Fixed message strings with only the HTTP status interpolated; `safe()` filter; `console.` allowed only in constants/verify sections (static gate); scenario logs:12 asserts no URL/payload/email/phone |
| T-01-10 | Tampering | `history.pushState` patch breaking HighLevel's router | medium | mitigate | Wrapper calls the original with the same `this`/args and returns its value; dispatch wrapped in try/catch; scenario nav:5 proves location/history still update |
| T-01-11 | Repudiation | no-cors retry cannot see the response | low | accept | Reported honestly as `Sent (unconfirmed)` with instructions to check the workflow log (D-11); v2 companion service gives a confirmed path |
| T-01-04 | Spoofing / Denial of Service | public Inbound Webhook URL | medium | accept | Accepted per D-08; HTTPS-only enforced at render (`isSafeHttpsUrl`), per-click `requestId` for downstream dedupe |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs in this plan; `package.json` still declares no dependencies |
</threat_model>

<verification>
- `node test/run.mjs` prints `PASS n/n` including every `nav:`, `stale:`, `ctx:`, `webhook:`, and `logs:` scenario.
- Grep gates in both tasks hold; `node --check` passes.
</verification>

<success_criteria>
- ROADMAP success criterion 2 (no duplicate sends; failed state never reveals URL/payload) and criterion 3 (button always bound to the on-screen contact across pushState, back/forward, and switcher navigation; refuses to fire when stale; absent without a contact and on agency pages) are true and automated.
- LOC-01..05, BTN-04..07, BTN-10..12, DLV-04 implemented.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-context-workflow-buttons/01-02-SUMMARY.md` when done. Record the final `sendWebhook` outcome table, the exact message strings, and any shim behavior you had to change to make the navigation scenarios faithful.
</output>
