---
phase: quick-260925-cf3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/agency-config.json
  - test/run.mjs
  - README.md
  - package.json
  - src/ghl-customizer.js
  - src/ghl-customizer.css
autonomous: true
requirements: [DLV-01, DLV-02, DLV-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Opening HighLevel location Cp2XxBsRoyKS2CUf1Hwi shows the Xcelsior Health logo HighLevel already hosts in the sidebar, the customizer buttons render in navy #2d3e50, and the native white sidebar is untouched (no data-ghlc-theme marker, no --ghlc- sidebar properties)"
    - "Dummy Clinic (iDPNGKoFsjvf9wUCrk3V) behaves exactly as before: fixture logo on the pinned tag, full four-token theme"
    - "The REPLACE_WITH_LOCATION_ID placeholder no longer exists anywhere in the config, the README, or the suite, and the README describes the shipped sample truthfully"
    - "Every jsDelivr URL in README.md, config/agency-config.json, and src/ pins v0.1.2; package.json, VERSION, the head-comment Version line, and the stylesheet header all say 0.1.2; tag v0.1.1 still points at 1076b44985c406594f12d57dca8409478d5f9394"
    - "jsDelivr serves src/ghl-customizer.js, src/ghl-customizer.css, config/agency-config.json, and test/fixtures/logos/loc-a.svg at @v0.1.2 byte-identical to the git tag"
    - "npm test prints PASS 119/119 after every task; the suite still forbids http logo URLs, floating jsDelivr refs, and secret-like keys"
  artifacts:
    - "config/agency-config.json — Xcelsior Health entry keyed Cp2XxBsRoyKS2CUf1Hwi with theme.primary only; Dummy Clinic unchanged except its logo tag"
    - "test/run.mjs — config: check accepts any https location logo while keeping the Dummy Clinic fixture tag-pinned and jsDelivr refs pinned; sample: scenario boots Xcelsior Health; stylesheet header version gated"
    - "README.md — sample-description sentence, logoUrl row, and release step 2 updated; all URLs at v0.1.2"
    - "package.json 0.1.2, src/ghl-customizer.js (Version line, VERSION, DEFAULT_CONFIG_URL) 0.1.2, src/ghl-customizer.css header 0.1.2"
    - "Annotated git tag v0.1.2 on origin"
  key_links:
    - "DEFAULT_CONFIG_URL tag -> TAG/CDN in test/run.mjs -> README URLs, Dummy Clinic logo URL, package.json version (the version-consistency gate)"
    - "config locations.Cp2XxBsRoyKS2CUf1Hwi.logoUrl -> isSafeHttpsUrl / isSafeImageUrl (https only) -> branding candidate list -> sidebar mount"
    - "sample: scenario in test/run.mjs -> config/agency-config.json as served (bootWithConfig with the parsed sample)"
    - "git tag v0.1.2 -> cdn.jsdelivr.net/gh/robhparker/ghl-admin-theme@v0.1.2/* (sha256 byte comparison)"
---

<objective>
Add the Xcelsior Health location entry (id `Cp2XxBsRoyKS2CUf1Hwi`) to the shipped sample config using the logo HighLevel already hosts for that location, replacing the `REPLACE_WITH_LOCATION_ID` placeholder; keep the test suite's guarantees intact while letting a location logo be any https URL; update the README so it describes the sample truthfully; release v0.1.2 per the README's "Releasing a new version" steps; and prove jsDelivr serves the tag byte-for-byte.

Purpose: Xcelsior Health is the first real client onboarded onto the customizer (STATE.md "Next: per-client onboarding (Xcelsior Health first)"). The sample config is the production config (DLV-02), so this entry is what staff will see once the HighLevel snippet is moved to v0.1.2.

Output: updated `config/agency-config.json`, `test/run.mjs`, `README.md`, `package.json`, `src/ghl-customizer.js`, `src/ghl-customizer.css`; annotated tag `v0.1.2` pushed to origin and verified on jsDelivr.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@README.md
@config/agency-config.json
@package.json

Read these ranges of `test/run.mjs` before editing (the file is 3316 lines; do not read it whole):
- lines 24-34: SLUG/TAG/CDN derivation from `DEFAULT_CONFIG_URL` and `pkgVersion`
- lines 192-199: the version-consistency check (`static: DEFAULT_CONFIG_URL slug and tag match package.json and VERSION`)
- lines 250-280: the `readme:` check (every README jsDelivr URL must use SLUG and pin TAG)
- lines 303-378: the `config:` check (the sample-config gates this task adjusts)
- lines 1105-1113: `bootWithConfig` (serves an arbitrary config body to the real script)
- lines 1942-1960 and 2087-2095: the `logoIs` and `go` helpers used by the sample scenario
- lines 3218-3282: the `sample:` scenario (boots the shipped config at Dummy Clinic, then the placeholder, then the agency route)
- lines 2941 and 3052: the exact `GHLC.verify().theme` report shape for a primary-only location

Facts established by the orchestrator (do not re-derive):
- The logo URL `https://msgsndr-private.storage.googleapis.com/locationPhotos/096434c9-83e6-42da-9644-8a93aed64980.png` returns HTTP 200, `image/png`, 600x361, no alpha channel, no query string or signature. `isSafeImageUrl` (src line 480) accepts any https URL without credentials, so it is a valid `logoUrl`.
- The logo has an opaque white background, so the entry sets `theme.primary` `#2d3e50` only and never `sidebarBg`, `sidebarText`, or `navActive`; the native white sidebar stays.
- The DOM shim's image stub fires `load` for any `src` by default (test/dom-shim.mjs lines 818-836), so the new https URL resolves in the sample scenario exactly like the jsDelivr fixture did.
- Current version 0.1.1; tag `v0.1.1` = `1076b44985c406594f12d57dca8409478d5f9394` and is never moved. Origin is `https://github.com/robhparker/ghl-admin-theme.git`, branch `main`.
- `npm test` prints `PASS 119/119` on HEAD today. No checks are added or removed by this plan (only modified), so the count stays 119.
- Untracked, never-to-be-pushed paths exist in the working tree (`.gsd/`, `.playwright-mcp/`, `docs/`, `.planning/state.json`). Stage files by explicit path only; never `git add -A` or `git add .`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the placeholder with the Xcelsior Health entry, adapt the suite's sample gates, and make the README truthful</name>
  <files>config/agency-config.json, test/run.mjs, README.md</files>
  <precondition>`npm test` prints `PASS 119/119` on the current HEAD (534c340) before any edit.</precondition>
  <action>
<!-- planner-discipline-allow: REPLACE_WITH_LOCATION_ID -->
<!-- planner-discipline-allow: Example Clinic -->
<!-- planner-discipline-allow: SAMPLE_LOC_B -->
<!-- planner-discipline-allow: SAMPLE_PLACEHOLDER -->

**config/agency-config.json.** Delete the whole `REPLACE_WITH_LOCATION_ID` object (the second key under `locations`) and put in its place a `Cp2XxBsRoyKS2CUf1Hwi` object with exactly four keys: `name` `"Xcelsior Health"`, `logoUrl` `"https://msgsndr-private.storage.googleapis.com/locationPhotos/096434c9-83e6-42da-9644-8a93aed64980.png"`, `logoAlt` `"Xcelsior Health"`, and `theme` `{ "primary": "#2d3e50" }`. No `sidebarBg`, `sidebarText`, `navActive`, or `buttons` key (the logo is opaque white, so the native sidebar must stay). Leave the Dummy Clinic entry, `agency`, and `buttons` byte-identical (its logo tag is bumped in Task 2, not here). Keep two-space indentation and the trailing newline; `locations` must still have exactly two keys.

**test/run.mjs, the `config:` check (lines 327-351).** Rewrite the Phase 3 sample-location block so it encodes the new sample while keeping every security rule:
- Update the comment (lines 327-330) to say the sample ships agency defaults plus exactly two location entries: Dummy Clinic on a repository SVG fixture at the published slug and tag with a full theme, and Xcelsior Health on the logo HighLevel hosts for the location with a `primary` accent only.
- Keep `assert.equal(Object.keys(sample.locations).length, 2, ...)`, the `dummy` lookup, and its `name` assertion.
- Replace the `placeholder` lookup and its existence assertion with `const xcelsior = sample.locations.Cp2XxBsRoyKS2CUf1Hwi;` followed by assertions that it exists, that `xcelsior.name` and `xcelsior.logoAlt` both equal `'Xcelsior Health'`.
- Replace the loop that runs `SAMPLE_LOGO_RE` over every location with two rules. Rule A (Dummy Clinic only): `SAMPLE_LOGO_RE.exec(dummy.logoUrl)` must match and the referenced fixture file must exist under `test/fixtures/logos/` (same two assertions as today, scoped to `dummy`). Rule B (every location): `api.isSafeHttpsUrl(entry.logoUrl)` must be true (https, no credentials; http and `javascript:` fail here), and if `entry.logoUrl` starts with `https://cdn.jsdelivr.net/gh/` it must also start with `CDN` (the published slug at the pinned TAG), so a floating jsDelivr reference (`@main`, `@latest`, or no `@tag`) fails with a message naming the location id. Also assert `xcelsior.logoUrl` starts with `https://msgsndr-private.storage.googleapis.com/` so the shipped sample is pinned to HighLevel's own bucket rather than an arbitrary host.
- Replace the two `placeholder.theme === undefined` / `placeholder.buttons === undefined` assertions with: `Object.keys(xcelsior.theme)` deep-equals `['primary']` (message: the logo is opaque, so only `primary` is set and the native sidebar stays), `api.parseColor(xcelsior.theme.primary)` is truthy, `xcelsior.buttons` is `undefined`, and `api.resolveTheme(sample, 'Cp2XxBsRoyKS2CUf1Hwi')` has `fallback === false` and an empty `ignored` array (mirror the existing `dummyTheme` assertions).
- Leave lines 352-378 untouched (the generic https gate over both files, the fixture-SVG checks including `loc-b.svg`, which `test/fixtures/config.json` still uses, and the `missing.svg` rule). Leave `SECRET_KEY_RE` and the secret-key loop untouched.

**test/run.mjs, the version-consistency check (lines 192-199).** Add one assertion so the README's claim that `npm test` checks every bump location becomes true for the stylesheet: read `src/ghl-customizer.css`, take its first 400 characters, and assert they include the string `version ` followed by `pkgVersion` (message: the stylesheet head comment version matches package.json). Today the header reads `version 0.1.1` and `pkgVersion` is `0.1.1`, so this passes before Task 2 and guards Task 2.

**test/run.mjs, the `sample:` scenario (lines 3223-3281).** Rename `SAMPLE_PLACEHOLDER` to `SAMPLE_XCELSIOR` with value `'Cp2XxBsRoyKS2CUf1Hwi'`. Replace `SAMPLE_LOC_B` with `SAMPLE_XCELSIOR_LOGO` holding the HighLevel-hosted URL above (a literal string, not derived from `CDN`). Replace `SAMPLE_AGENCY_PRIMARY` with `SAMPLE_XCELSIOR_PRIMARY = '#2d3e50'` (the agency constant becomes unused, so remove it). Extend `SAMPLE_LEAKS` with `'Xcelsior'`, `'#2d3e50'`, `'msgsndr'`, `'googleapis'`, and `'locationPhotos'` so neither the new logo URL nor the new color can ever reach the console (DLV-04). In the "placeholder location" section: retitle the comment to the Xcelsior Health location (HighLevel-hosted logo, `primary` only, native sidebar), navigate with `go(shim, \`/v2/location/${SAMPLE_XCELSIOR}/dashboard\`)`, assert `logoIs(shell.logo, { src: SAMPLE_XCELSIOR_LOGO, alt: 'Xcelsior Health', tier: 'location' })`, assert every group's `--ghlc-primary` equals `SAMPLE_XCELSIOR_PRIMARY` (the location overrides the agency `#155eef`), keep the two existing assertions that the sidebar carries no `data-ghlc-theme` marker and no `--ghlc-` custom property (that is the "native sidebar stays" proof), and add: `plain(GHLC.verify().theme)` deep-equals `{ root: true, applied: ['primary'], navActive: 0, ignored: 0, fallback: false }` and `GHLC.verify().branding.applied` equals `'location'`. Leave the Dummy Clinic and agency-route sections untouched.

**README.md.** Three scoped edits, nothing else (in particular do not touch the "Example location entry" JSON or any H2 heading):
1. The sentence after the example entry (line 143) becomes: "The shipped sample, `config/agency-config.json`, demonstrates agency defaults, two location entries (Dummy Clinic with a full theme and a repository-fixture logo, and Xcelsior Health with the logo HighLevel already hosts for it and a `primary` accent only, which leaves the native sidebar untouched), the `helpCenter` header link, and the `sendInvite` webhook button."
2. In the `locations[id].logoUrl` row of the configuration table (line 80), append to the Meaning cell: "Any `https` image URL works, including the logo HighLevel already hosts for the location's Business Profile (the sample's Xcelsior Health entry); if that logo is re-uploaded in HighLevel, confirm the URL still resolves."
3. In "Releasing a new version" step 2 (line 46), replace the clause "the tag in both logo URLs in `config/agency-config.json`" with "the version in the head comment of `src/ghl-customizer.css`; the tag in the Dummy Clinic logo URL in `config/agency-config.json` (the Xcelsior Health logo is hosted by HighLevel and carries no tag)". Keep the rest of the sentence, the grep command, and the `npm test` claim (now true for the stylesheet too).

Run `npm test`; it must print `PASS 119/119`. Then commit only these three files by explicit path: `git add config/agency-config.json test/run.mjs README.md` and commit as `feat(config): add Xcelsior Health location entry, drop the placeholder`. Never `git add -A`.
  </action>
  <verify>
    <automated>cd /Users/rhparker/Repos/ghl/admin-theme && npm test 2>&1 | tail -1 | grep -qx 'PASS 119/119' && ! grep -rn 'REPLACE_WITH_LOCATION_ID\|Example Clinic\|SAMPLE_PLACEHOLDER\|SAMPLE_LOC_B\b\|SAMPLE_AGENCY_PRIMARY' config/agency-config.json README.md test/run.mjs && node -e "const c=JSON.parse(require('fs').readFileSync('config/agency-config.json','utf8'));const x=c.locations.Cp2XxBsRoyKS2CUf1Hwi;const d=c.locations.iDPNGKoFsjvf9wUCrk3V;if(!x||x.name!=='Xcelsior Health'||x.logoAlt!=='Xcelsior Health'||!x.logoUrl.startsWith('https://msgsndr-private.storage.googleapis.com/locationPhotos/')||JSON.stringify(Object.keys(x.theme))!=='[\"primary\"]'||x.theme.primary!=='#2d3e50'||x.buttons!==undefined||Object.keys(c.locations).length!==2||!d||Object.keys(d.theme).length!==4)process.exit(1)" && grep -q 'Xcelsior Health with the logo HighLevel already hosts' README.md && grep -q 'the tag in the Dummy Clinic logo URL' README.md && grep -q "stylesheet head comment version" test/run.mjs && git diff --quiet HEAD -- config/agency-config.json test/run.mjs README.md</automated>
  </verify>
  <done>`npm test` prints `PASS 119/119`; `config/agency-config.json` has exactly two locations, Dummy Clinic unchanged and `Cp2XxBsRoyKS2CUf1Hwi` with name/alt "Xcelsior Health", the HighLevel-hosted https logo, and `theme.primary` `#2d3e50` only; the placeholder key and its alt text appear nowhere in the config, README, or suite; the suite requires Dummy Clinic's logo to be the tag-pinned fixture, every location logo to be credential-free https, any jsDelivr logo to be pinned at TAG, and the stylesheet header version to match package.json; the `sample:` scenario boots Xcelsior Health with its logo, navy buttons, and an unthemed sidebar; README describes the two-entry sample truthfully; the three files are committed.</done>
</task>

<task type="auto">
  <name>Task 2: Bump every version marker from 0.1.1 to 0.1.2</name>
  <files>package.json, src/ghl-customizer.js, src/ghl-customizer.css, config/agency-config.json, README.md</files>
  <action>
<!-- planner-discipline-allow: 0.1.1 -->

Follow README "Releasing a new version" step 2 exactly, changing only the version/tag text at each site; no other edits:
- `package.json`: `"version": "0.1.1"` becomes `"0.1.2"`.
- `src/ghl-customizer.js`: line 3 head-comment `Version 0.1.1` becomes `Version 0.1.2`; line 44 `VERSION = '0.1.1'` becomes `'0.1.2'`; line 49 `DEFAULT_CONFIG_URL` tag `@v0.1.1/` becomes `@v0.1.2/`.
- `src/ghl-customizer.css`: line 2 header `version 0.1.1` becomes `version 0.1.2`.
- `config/agency-config.json`: the Dummy Clinic `logoUrl` tag `@v0.1.1/` becomes `@v0.1.2/` (the only tag-carrying URL left in the config; the Xcelsior Health URL carries no tag and must not change).
- `README.md`: every jsDelivr URL that carries `@v0.1.1/` becomes `@v0.1.2/` (the `<script>` snippet's `src` and `data-config`, the JavaScript loader's `s.src` and `data-config`, and the "Example location entry" `logoUrl`). The prose `grep -rn 'ghl-admin-theme@v' ...` command in step 2 carries no version and stays as is.

`grep -rn 'ghl-admin-theme@v' README.md config src` is the authoritative list of tag URLs; after the edit every match must read `@v0.1.2/` and nothing under `package.json`, `src/`, `config/`, or `README.md` may still contain `0.1.1`. Run `npm test`: the version-consistency gate derives TAG from `DEFAULT_CONFIG_URL`, checks it against `package.json`, `VERSION`, the head comment, and (from Task 1) the stylesheet header; the `readme:` and `config:` gates re-check every pinned URL. It must print `PASS 119/119`.

Commit only these five files by explicit path: `git add package.json src/ghl-customizer.js src/ghl-customizer.css config/agency-config.json README.md`, message `chore(release): bump to v0.1.2`. Do not create the tag in this task.
  </action>
  <verify>
    <automated>cd /Users/rhparker/Repos/ghl/admin-theme && npm test 2>&1 | tail -1 | grep -qx 'PASS 119/119' && [ "$(grep -rnoE 'ghl-admin-theme@v[0-9]+\.[0-9]+\.[0-9]+' README.md config src | grep -vc '@v0\.1\.2$')" = 0 ] && [ "$(grep -rnoE 'ghl-admin-theme@v[0-9]+\.[0-9]+\.[0-9]+' README.md config src | wc -l | tr -d ' ')" = 7 ] && ! grep -rn '0\.1\.1' package.json src config README.md && grep -q '"version": "0.1.2"' package.json && grep -q "VERSION = '0.1.2'" src/ghl-customizer.js && grep -q '^ \* Version 0.1.2$' src/ghl-customizer.js && grep -q 'version 0.1.2' src/ghl-customizer.css && git diff --quiet HEAD -- package.json src config README.md</automated>
  </verify>
  <done>All seven tag-carrying jsDelivr URLs (README x5, config x1, src x1) read `@v0.1.2/`; `package.json`, `VERSION`, the head-comment `Version` line, and the stylesheet header all say 0.1.2; no `0.1.1` remains in package.json, src/, config/, or README.md; `npm test` prints `PASS 119/119`; the bump is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Tag v0.1.2, push main with tags, and prove jsDelivr serves the tag byte-for-byte</name>
  <files>none (git tag and push only; no working-tree edits, so there is nothing to commit in this task)</files>
  <precondition>`git tag -l v0.1.2` prints nothing, `git ls-remote --tags origin refs/tags/v0.1.2` prints nothing, `git status --porcelain --untracked-files=no` prints nothing (all tracked changes committed), and `git show HEAD:package.json` contains `"version": "0.1.2"`.</precondition>
  <reversibility rating="costly">A tag served by jsDelivr is cached forever and is never moved (Phase 3 decision). A bad release is undone by pointing the HighLevel snippet back at v0.1.1 and cutting v0.1.3, not by deleting or re-pointing v0.1.2.</reversibility>
  <action>
1. Confirm the precondition commands above and that `git rev-list -n1 v0.1.1` still prints `1076b44985c406594f12d57dca8409478d5f9394`. Halt if any fails.
2. Create the annotated tag on HEAD: `git tag -a v0.1.2 -m "GHL Customizer v0.1.2 — Xcelsior Health location entry"`.
3. Push the branch and tags together: `git push origin main --tags`. Only commits and tags travel; the untracked `.gsd/`, `.playwright-mcp/`, `docs/`, and `.planning/state.json` stay local. Confirm with `git ls-remote --tags origin refs/tags/v0.1.2`.
4. Wait for jsDelivr without a foreground sleep: `curl -sS -f --retry 30 --retry-delay 10 --retry-all-errors -o /dev/null -w '%{http_code}\n' https://cdn.jsdelivr.net/gh/robhparker/ghl-admin-theme@v0.1.2/config/agency-config.json` (up to ~5 minutes; run it with a 420000 ms tool timeout). It must end with `200`. If it still fails after the retries, request a cache purge once with `curl -sS https://purge.jsdelivr.net/gh/robhparker/ghl-admin-theme@v0.1.2/config/agency-config.json` and repeat the retry command once.
5. Byte-compare the served files with the tag for `src/ghl-customizer.js`, `src/ghl-customizer.css`, `config/agency-config.json`, and `test/fixtures/logos/loc-a.svg`: for each path compute `git show v0.1.2:<path> | shasum -a 256` and `curl -sSf https://cdn.jsdelivr.net/gh/robhparker/ghl-admin-theme@v0.1.2/<path> | shasum -a 256`; every pair must be identical. Also parse the served config with `node -e` and confirm `locations.Cp2XxBsRoyKS2CUf1Hwi.theme.primary` is `#2d3e50` and `locations` has exactly two keys.
6. Record the four sha256 values and the final HTTP status in the SUMMARY. The HighLevel snippet is NOT changed by this task; note in the SUMMARY that Rob switches both snippet URLs to `@v0.1.2` per README step 5 and then runs `GHLC.verify()` at the Xcelsior Health location.
  </action>
  <verify>
    <automated>cd /Users/rhparker/Repos/ghl/admin-theme && git rev-parse -q --verify 'refs/tags/v0.1.2^{commit}' >/dev/null && git ls-remote --tags origin refs/tags/v0.1.2 | grep -q 'refs/tags/v0.1.2' && [ "$(git rev-list -n1 v0.1.1)" = 1076b44985c406594f12d57dca8409478d5f9394 ] && git cat-file -e v0.1.2:package.json && TAGPKG=$(git cat-file -p v0.1.2:package.json) && [ "$(printf '%s' "$TAGPKG" | grep -c '"version": "0.1.2"')" = 1 ] && [ "$(curl -sS -o /dev/null -w '%{http_code}' https://cdn.jsdelivr.net/gh/robhparker/ghl-admin-theme@v0.1.2/config/agency-config.json)" = 200 ] && for f in src/ghl-customizer.js src/ghl-customizer.css config/agency-config.json test/fixtures/logos/loc-a.svg; do git cat-file -e "v0.1.2:$f" || { echo "MISSING-AT-TAG $f"; exit 1; }; a=$(git cat-file -p "v0.1.2:$f" | shasum -a 256 | cut -d' ' -f1); b=$(curl -sSf "https://cdn.jsdelivr.net/gh/robhparker/ghl-admin-theme@v0.1.2/$f" | shasum -a 256 | cut -d' ' -f1) || { echo "CDN-FETCH-FAILED $f"; exit 1; }; [ -n "$a" ] && [ "$a" = "$b" ] || { echo "MISMATCH $f"; exit 1; }; done && SERVED=$(curl -sSf https://cdn.jsdelivr.net/gh/robhparker/ghl-admin-theme@v0.1.2/config/agency-config.json) && printf '%s' "$SERVED" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const c=JSON.parse(s);if(Object.keys(c.locations).length!==2||c.locations.Cp2XxBsRoyKS2CUf1Hwi.theme.primary!=='#2d3e50')process.exit(1)})" && echo TAG-SERVED-AND-BYTE-IDENTICAL</automated>
  </verify>
  <done>Annotated tag `v0.1.2` exists on origin and points at a commit whose `package.json` says 0.1.2; `v0.1.1` is unchanged; `https://cdn.jsdelivr.net/gh/robhparker/ghl-admin-theme@v0.1.2/config/agency-config.json` returns HTTP 200; the served js, css, config, and loc-a.svg are sha256-identical to `git show v0.1.2:<path>`; the served config carries the Xcelsior Health entry with `primary` `#2d3e50`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HighLevel storage bucket -> staff browser | The Xcelsior Health logo is fetched from a Google Cloud Storage object HighLevel controls, not from this repository |
| GitHub tag -> jsDelivr -> staff browser | Every staff session executes whatever bytes jsDelivr serves for the pinned tag |
| Local working tree -> origin | `git push` publishes commits to a public repository |
| Config values -> console | Diagnostics must never carry URLs or colors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q1-01 | Spoofing / Information disclosure | `locations.Cp2XxBsRoyKS2CUf1Hwi.logoUrl` (third-party host) | low | accept | Host is HighLevel's own bucket for this location's Business Profile; `isSafeImageUrl` (src line 480) refuses non-https and credentialed URLs; logo `<img>` carries `referrerpolicy="no-referrer"`; Task 1 pins the sample's host to `msgsndr-private.storage.googleapis.com/` in the `config:` gate |
| T-Q1-02 | Denial of service | Logo URL invalidated by a HighLevel re-upload | low | accept | Phase 2 BRD-04 fallback: a load error drops to the agency logo, then the native logo; README row now tells the operator to re-check the URL after a re-upload |
| T-Q1-03 | Tampering (supply chain) | Floating jsDelivr references in README/config/src | high | mitigate | Task 2 pins all seven tag URLs to `@v0.1.2/`; suite gates (`readme:` check, `config:` rule B, version-consistency check) fail on any unpinned or mismatched URL; Task 2 verify counts exactly seven pinned URLs |
| T-Q1-04 | Tampering | Bytes served by jsDelivr differ from the tag | medium | mitigate | Task 3 compares sha256 of `git show v0.1.2:<path>` against the CDN for js, css, config, and loc-a.svg before the task is done |
| T-Q1-05 | Information disclosure | Untracked local files (`.gsd/`, `.playwright-mcp/`, `docs/`, `.planning/state.json`) reaching the public repo | medium | mitigate | Every commit stages by explicit path; `git add -A` is forbidden in all three tasks; `git push` transports only commits and tags |
| T-Q1-06 | Information disclosure | New logo URL or color reaching the console (DLV-04) | low | mitigate | Task 1 extends `SAMPLE_LEAKS` with `Xcelsior`, `#2d3e50`, `msgsndr`, `googleapis`, `locationPhotos`; the `sample:` scenario asserts no leak |
| T-Q1-07 | Tampering | Secret-like keys or http logo URLs slipping into the sample | high | mitigate | `SECRET_KEY_RE` loop and the https-only logo gate (lines 352-358) are left untouched; Task 1 rule B re-asserts `isSafeHttpsUrl` per location |
| T-Q1-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this task (zero dependencies; `npm test` runs `node test/run.mjs` only) |
</threat_model>

<verification>
- After each task: `npm test` prints `PASS 119/119` (count unchanged: checks are modified, none added or removed).
- After Task 1: the placeholder key and alt text are absent from `config/agency-config.json`, `README.md`, and `test/run.mjs`; the sample scenario boots the Xcelsior Health dashboard with the HighLevel-hosted logo, `--ghlc-primary` `#2d3e50` on every group, and a sidebar with no `data-ghlc-theme` marker.
- After Task 2: `grep -rnoE 'ghl-admin-theme@v[0-9]+\.[0-9]+\.[0-9]+' README.md config src` lists exactly seven matches, all `@v0.1.2`; no `0.1.1` in package.json, src/, config/, README.md.
- After Task 3: `git ls-remote --tags origin` shows `v0.1.2`; `v0.1.1` still resolves to `1076b44985c406594f12d57dca8409478d5f9394`; the four served files are sha256-identical to the tag.
- Follow-up outside this plan: the orchestrator's docs commit (PLAN/SUMMARY/STATE) lands after the push in Task 3, so `main` will be one docs commit ahead of origin until the orchestrator pushes; the tag content is unaffected.
</verification>

<success_criteria>
- `config/agency-config.json` ships Dummy Clinic (unchanged, logo at `@v0.1.2`) and Xcelsior Health (`Cp2XxBsRoyKS2CUf1Hwi`, HighLevel-hosted logo, `theme.primary` `#2d3e50` only); no placeholder remains.
- The suite still forbids http and credentialed logo URLs, floating jsDelivr references, and secret-like keys, and now also gates the stylesheet header version.
- README describes the two-entry sample truthfully and lists every bump site; all README URLs pin `v0.1.2`.
- Version 0.1.2 is consistent across package.json, the script, the stylesheet, the config, and the README; tag `v0.1.2` is on origin; jsDelivr serves it byte-identical; `v0.1.1` is untouched.
- Rob's remaining manual step (not in this plan): switch both HighLevel snippet URLs to `@v0.1.2` and run `GHLC.verify()` at the Xcelsior Health location.
</success_criteria>

<output>
Create `.planning/quick/260925-cf3-add-an-xcelsior-health-location-entry-id/260925-cf3-SUMMARY.md` when done (frontmatter `status: complete`; include the four sha256 pairs and the final jsDelivr HTTP status from Task 3).
</output>
